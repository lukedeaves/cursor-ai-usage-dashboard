# Desktop App (optional)

Native window wrapper using Python + pywebview. Falls back to your default browser if pywebview is not installed.

```bash
pip install pywebview   # optional
python3 desktop/run.py
```

The script starts a local Python HTTP server and opens the dashboard at `http://127.0.0.1:8765`.
